services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_db_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
    restart: always

# Note: Using a separate directory chroma_db_data to avoid conflicts with existing chroma_db folder
